#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    cout << INT32_MAX << endl;
    /*int firstNum, secondNum;
    cin >> firstNum >> secondNum;
    
    bool isFirstValid = firstNum > 0 && firstNum < 2400;
    bool isSecondValid = secondNum > 0 && secondNum < 2400;

    // 2300 0100 
    
    if (firstNum >= 0 && firstNum <= 959) {
        firstNum += 2400;
    }
    if (secondNum >= 0 && secondNum <= 959) {
        secondNum += 2400;
    }

    cout << abs(firstNum - secondNum); */
}
